{
  "name": "auto-enrich",
  "provider": "copilot",
  "defaults": {
    "model": "claude-sonnet-4.5"
  },
  "entry": "pull",
  "state": {
    "topic": "DePIN project Helium needs category, token, summary"
  },
  "nodes": [
    {
      "id": "pull",
      "type": "script",
      "code": "def pull(ctx):\n    return ctx.state.get('topic', '')",
      "output": {"name": "record", "type": "string"}
    },
    {
      "id": "enrich",
      "type": "agent",
      "inputs": ["record"],
      "system_prompt": "You are a data-enrichment agent. Fill missing fields precisely; do not invent facts.",
      "prompt": "Enrich this record, filling every field:\n\n{{record}}",
      "output": "enriched",
      "output_schema": {
        "category": "string",
        "token": "string",
        "summary": "string"
      }
    },
    {
      "id": "persist",
      "type": "script",
      "inputs": ["record"],
      "code": "def persist(ctx, record):\n    return record",
      "output": {"name": "saved", "type": "string"}
    }
  ],
  "edges": [
    {"from": "pull", "to": "enrich"},
    {"from": "enrich", "to": "persist"}
  ]
}
